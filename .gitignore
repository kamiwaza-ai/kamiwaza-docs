# Dependencies
/node_modules

# Production
/build
# Docusaurus offline build output (created by docs/scripts/build-offline.cjs).
# Anchored so it does not match docs/scripts/build-offline.cjs itself.
docs/build-offline/
dist

# Generated files
.docusaurus
.cache-loader

# Compiled TypeScript outputs (npm scripts run the .ts sources via ts-node).
/scripts/*.js

# SDK docs (synced from kamiwaza-sdk repo)
/docs/sdk/current/
/docs/sdk-services.generated.json

# Misc
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
docs/package-lock.json
package-lock.json

# PDF generation output - only save numbered versions
dist/pdf/*vcurrent.pdf
.DS_Store
*.DS_Store
.venv/
